return {
    -- "fatih/vim-go",
    -- ft = "go",
    -- build = ":GoInstallBinaries", -- 自动安装 gocode 等工具
    -- config = function()
    --     -- vim.g.go_fmt_command = "goimports-reviser" -- 替换原生goimports[1](@ref)
    --     -- vim.g.go_fmt_command = "goimports" -- 使用goimports替代gofmt[3,4](@ref)
    --     vim.g.go_highlight_types = 1     -- 启用类型高亮
    --     -- vim.g.go_metalinter_autosave = 1 -- 保存时自动静态检查
    --     vim.g.go_metalinter_autosave = 0 -- 禁用vim-go内置静态检查
    -- end,
}
